language: node_js

branches:
  except:
    - whateverbranch

node_js:
  - "10"
  - "12"
  - "13"
  - "14"

runtime:
  nodePool: shippable_shared_aarch64

build:
  ci:
    - npm install extract-zip --save
    - node install.js
